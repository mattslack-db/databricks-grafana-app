# Grafana on Databricks Apps (Lakebase)

Run Grafana OSS as a prebuilt binary inside a Databricks App, backed by Lakebase Postgres,
with token rotation handled by a local PgBouncer + Python refresher.

> Full build, deploy, and operational instructions will be filled in Task 11.
